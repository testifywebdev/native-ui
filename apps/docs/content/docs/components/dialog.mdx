---
title: Dialog
description: A modal overlay for displaying content that requires user interaction, with animated entrance/exit, an optional close button, and a backdrop.
---

import { CommandTabs } from "@//components/command-tabs";
import { ExternalLinks } from "@//components/external-links";
import { PortalInfoAlert } from "@//components/portal-info-alert";
import { PreviewCard } from "@//components/preview-card";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@//components/ui/tabs";
import { Step, Steps } from 'fumadocs-ui/components/steps';

<ExternalLinks
  links={[
    { title: 'RN Primitives Docs', url: 'https://rnprimitives.com/dialog/' },
    { title: 'RN Primitives API Reference', url: 'https://rnprimitives.com/dialog/#props' },
  ]}
/>

<div className="h-5"/>

<Tabs defaultValue="code">
  <TabsList>
    <TabsTrigger value="preview">Preview</TabsTrigger>
    <TabsTrigger value="code">Code</TabsTrigger>
  </TabsList>
  <TabsContent value="preview">
    <PreviewCard />
  </TabsContent>
  <TabsContent value="code">
    ```tsx
    import {
      Dialog,
      DialogTrigger,
      DialogContent,
      DialogHeader,
      DialogFooter,
      DialogTitle,
      DialogDescription,
    } from '@/components/ui/dialog';
    import Button from '@/components/ui/button';

    export function DialogDemo() {
      return (
        <Dialog>
          <DialogTrigger asChild>
            <Button title="Open Dialog" />
          </DialogTrigger>
          <DialogContent>
            <DialogHeader>
              <DialogTitle>Edit Profile</DialogTitle>
              <DialogDescription>
                Make changes to your profile here. Click save when you're done.
              </DialogDescription>
            </DialogHeader>
            <DialogFooter>
              <Button title="Save Changes" />
            </DialogFooter>
          </DialogContent>
        </Dialog>
      );
    }
    ```
  </TabsContent>
</Tabs>

## Installation

<Tabs defaultValue="cli">
  <TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="cli">
    <Steps>
      <Step>
        <CommandTabs args={["add", "dialog"]} />
      </Step>
    </Steps>
  </TabsContent>
  <TabsContent value="manual">
    <Steps>
      <Step>
        **Install the required dependencies:**
        ```bash
        npx expo install @expo/vector-icons @rn-primitives/dialog react-native-reanimated react-native-screens
        ```
      </Step>
      <Step>
        **Copy and paste the following code into `components/ui/dialog.tsx`.**
        <ComponentSource name="dialog" />
      </Step>
      <Step>
        **Update the import paths to match your project setup.**
      </Step>
    </Steps>
  </TabsContent>
</Tabs>

<PortalInfoAlert />

## Usage

```tsx
import {
  Dialog,
  DialogTrigger,
  DialogContent,
  DialogHeader,
  DialogFooter,
  DialogTitle,
  DialogDescription,
  DialogClose,
} from '@/components/ui/dialog';
```

```tsx
<Dialog>
  <DialogTrigger asChild>
    <Button title="Open" />
  </DialogTrigger>
  <DialogContent>
    <DialogHeader>
      <DialogTitle>Dialog Title</DialogTitle>
      <DialogDescription>A description of what this dialog does.</DialogDescription>
    </DialogHeader>
    {/* content */}
    <DialogFooter>
      <DialogClose asChild>
        <Button title="Close" variant="outline" />
      </DialogClose>
    </DialogFooter>
  </DialogContent>
</Dialog>
```

## Examples

### Without close button

```tsx
<DialogContent showCloseButton={false}>
  <DialogTitle>No Close Button</DialogTitle>
  <DialogDescription>Users must use the footer to dismiss.</DialogDescription>
  <DialogFooter>
    <DialogClose asChild>
      <Button title="Done" />
    </DialogClose>
  </DialogFooter>
</DialogContent>
```

### Controlled dialog

```tsx
const [open, setOpen] = useState(false);

<Dialog open={open} onOpenChange={setOpen}>
  <DialogTrigger asChild>
    <Button title="Open" onPress={() => setOpen(true)} />
  </DialogTrigger>
  <DialogContent>
    <DialogTitle>Controlled</DialogTitle>
    <DialogFooter>
      <Button title="Close" onPress={() => setOpen(false)} />
    </DialogFooter>
  </DialogContent>
</Dialog>
```

### With a form

```tsx
<Dialog>
  <DialogTrigger asChild>
    <Button title="Edit Details" />
  </DialogTrigger>
  <DialogContent>
    <DialogHeader>
      <DialogTitle>Edit Details</DialogTitle>
    </DialogHeader>
    <Input label="Name" value={name} onChangeText={setName} />
    <Input label="Email" value={email} onChangeText={setEmail} inputMode="email" />
    <DialogFooter>
      <DialogClose asChild>
        <Button title="Cancel" variant="outline" />
      </DialogClose>
      <Button title="Save" onPress={handleSave} />
    </DialogFooter>
  </DialogContent>
</Dialog>
```

## API Reference

### Dialog

Root component. Manages open/closed state.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `open` | `boolean` | — | Controlled open state. |
| `onOpenChange` | `(open: boolean) => void` | — | Callback when open state changes. |

### DialogTrigger

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `asChild` | `boolean` | `false` | Merge props onto the child element. |

### DialogContent

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `showCloseButton` | `boolean` | `true` | Shows the × button in the top-right corner. |
| `portalHost` | `string` | — | Custom portal host name. |
| `accessibilityLabel` | `string` | `"Dialog"` | Screen reader label for the modal. |
| `style` | `ViewStyle` | — | Additional styles for the dialog panel. |

### DialogHeader

Container for `DialogTitle` and `DialogDescription`.

### DialogFooter

A right-aligned row for action buttons.

### DialogTitle

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `style` | `TextStyle` | — | Additional text styles. |

### DialogDescription

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `style` | `TextStyle` | — | Additional text styles. |

### DialogClose

Wraps a child element and closes the dialog when it is pressed.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `asChild` | `boolean` | `false` | Merge props onto the child element. |
